<?xml version="1.0" encoding="utf-8"?>
    <resources>
        <style name="Theme.MyLearnApplication" parent="Theme.AppCompat.Light.NoActionBar">
            <!-- Tùy chỉnh theme tại đây nếu cần -->
            <item name="colorPrimary">@color/purple_500</item>
            <item name="android:windowBackground">@color/white</item>
        </style>
    </resources>